name: Build&Deploy

on: workflow_dispatch

jobs:
  build:

    runs-on: self-hosted
    environment: PROD
    steps:
      - uses: actions/checkout@v4.1.1
      - name: Build & Deploy
        run: echo "$ENV"
